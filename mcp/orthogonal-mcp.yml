generated: '2026-08-14'
method: searched
source: https://docs.orthogonal.com/mcp/setup.md
status: published
status_raw: official
name: Orthogonal
description: Official hosted, remote MCP server that gives any MCP-compatible client (Claude, Cursor,
  etc.) natural-language discovery and pay-per-call execution across Orthogonal's catalog of 40+ third-party
  APIs.
transport: streamable-http
url: https://mcp.orthogonal.com
authorization:
  type: oauth2
  authorization_server: https://mcp.orthogonal.com/.well-known/oauth-authorization-server
  issuer: https://clerk.orthogonal.com
  flow: authorization_code
  pkce: S256
  dynamic_client_registration: https://mcp.orthogonal.com/oauth/register
tools:
- name: search
  description: Find APIs by describing what you need in natural language.
- name: get_details
  description: Retrieve full parameter info for an endpoint.
- name: quote
  description: Get pricing for a call without executing it.
- name: use
  description: Execute an API call.
- name: integrate
  description: Get ready-to-use code snippets for an endpoint.
- name: batch_use
  description: Run up to 20 API calls in parallel.
- name: batch_get_details
  description: Fetch parameter info for up to 20 endpoints simultaneously.
clients:
  claude_desktop: Settings -> Connectors -> Add custom connector -> URL https://mcp.orthogonal.com
  cursor: Settings -> MCP -> Add Server -> URL https://mcp.orthogonal.com
  generic: '{"mcpServers":{"orthogonal":{"url":"https://mcp.orthogonal.com"}}}'
introspection:
  method: POST tools/list
  endpoint: https://mcp.orthogonal.com/
  http_status: 401
  response: '{"jsonrpc":"2.0","id":1,"error":{"code":-32001,"message":"Unauthorized","data":"Missing authentication. Use x-api-host + x-api-key headers for user MCPs, or Bearer token for Orthogonal MCP."}}'
  checked: '2026-08-14'
  note: >-
    Live tools/list is auth-gated, so the tool list above is the PUBLISHED one
    (docs.orthogonal.com/mcp/setup) — names and descriptions only. Per-tool
    inputSchemas require authenticated introspection and are not recorded. The
    anonymous discovery documents WERE readable: RFC 8414 authorization-server
    metadata and RFC 9728 protected-resource metadata both return 200 (see
    well-known/orthogonal-well-known.yml).
protected_resource:
  metadata: https://mcp.orthogonal.com/.well-known/oauth-protected-resource
  spec: RFC 9728
  resource: https://mcp.orthogonal.com
  authorization_servers:
  - https://mcp.orthogonal.com
  scopes_supported:
  - openid
  - profile
  - email
  bearer_methods_supported:
  - header
  http_status: 200
  file: ../well-known/orthogonal-mcp-oauth-protected-resource.json
byo_mcp:
  note: >-
    The 401 body reveals a second mode: "user MCPs" authenticated with x-api-host
    + x-api-key headers, alongside the first-party Orthogonal MCP authenticated
    with a Bearer token. Not documented on the setup page; recorded as observed.
crosswalk: mcp/orthogonal-tool-crosswalk.yml
deployment:
  mode: remote
  endpoint: https://mcp.orthogonal.com
  auth: oauth
  verified: probed
  tools: 7
  checked: '2026-08-14'
  source: >-
    Probed 2026-08-14 — POST https://mcp.orthogonal.com/ answers JSON-RPC with an
    auth challenge (401), and GET /openapi.json returns an endpoint map
    advertising 'POST /' (JSON-RPC) and 'GET /sse'. Remote-only: Orthogonal ships
    no stdio MCP package, and the documented client config is a bare URL.
